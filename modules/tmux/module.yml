name: tmux
description: Terminal multiplexer with TPM plugin manager (binary only; bring your own config)
version: 1.1.0
priority: 35
dependencies: []
os: []
prompts: []
legacy_paths:
  - path: "~/.tmux.conf"
    action: backup
    reason: "Shadows XDG config (~/.config/tmux/tmux.conf) on tmux 3.1+"
tags:
  - terminal
  - productivity
