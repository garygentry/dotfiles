name: tmux
description: Terminal multiplexer with TPM plugin manager
version: 1.0.0
priority: 35
dependencies: []
os: []
prompts:
  - key: tmux_config
    message: "Tmux config preset (opinionated = prefix Ctrl-j/f, vi keys, theme)"
    default: "skip"
    type: choice
    options:
      - skip
      - opinionated
    show_when: explicit_install
tags:
  - terminal
  - productivity
