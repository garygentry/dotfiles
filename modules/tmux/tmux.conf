# ==========================================
# TL;DR: Tmux Configuration
# Prefix: Ctrl-j or Ctrl-f
# Reload: Prefix + r
# ==========================================

# --- Initial Setup & Performance ---
set -g default-terminal "tmux-256color"
set -as terminal-features ",xterm-256color:RGB" # True color support (matches outer TERM)
set -g status-keys vi
set -g history-limit 50000                      # Match tmux-sensible default
set -sg escape-time 0                           # No delay for escape key press
set-option -g repeat-time 300                   # Allow short window for repeating commands
set -g mouse on                                 # Enable mouse support

# --- Prefix Configuration ---
set-option -g prefix C-j
set-option -g prefix2 C-f
bind-key C-j send-prefix

# --- Window & Pane Indexing ---
set -g base-index 1                             # Start windows at 1
set -g pane-base-index 1                        # Start panes at 1
set -g renumber-windows on                      # Automatically renumber windows when one closes

# --- Navigation & Splits ---

# Smart splits (opens in current directory)
bind-key v split-window -h -c "#{pane_current_path}"
bind-key h split-window -v -c "#{pane_current_path}"
bind c new-window -c "#{pane_current_path}"

# Alt-arrow keys (No prefix) to switch panes
bind -n M-Left select-pane -L
bind -n M-Right select-pane -R
bind -n M-Up select-pane -U
bind -n M-Down select-pane -D

# Shift-arrow to switch windows
bind -n S-Left  previous-window
bind -n S-Right next-window

# Reorder windows with CTRL+SHIFT+Arrow
bind-key -n C-S-Left swap-window -t -1 \; select-window -t -1
bind-key -n C-S-Right swap-window -t +1 \; select-window -t +1

# Resize panes (Prefix + H, J, K, L)
bind -r H resize-pane -L 5
bind -r J resize-pane -D 5
bind -r K resize-pane -U 5
bind -r L resize-pane -R 5

# --- Utility Commands ---

# Synchronize panes toggle
bind-key y set-window-option synchronize-panes\; display-message "Synchronize mode toggled."

# Easy config reload
bind-key r source-file ~/.config/tmux/tmux.conf \; display-message "tmux.conf reloaded."

# Improved Clear History (Prefix + Ctrl-l) — avoids collision with resize-right on L
bind C-l send-keys C-l \; run 'sleep 0.1' \; clear-history

# --- Copy Mode (Vi Style) ---
setw -g mode-keys vi
unbind p
bind p paste-buffer
bind-key -T copy-mode-vi 'v' send -X begin-selection

# Clipboard integration (auto-detects macOS vs Linux)
if-shell "command -v pbcopy" \
  "bind-key -T copy-mode-vi 'y' send -X copy-pipe-and-cancel 'pbcopy'" \
  "bind-key -T copy-mode-vi 'y' send -X copy-pipe-and-cancel 'xclip -selection clipboard -i'"

# --- Visuals & Status Bar ---

# Title & Renaming
set-window-option -g automatic-rename on
set-option -g set-titles on

# Message display time
set-option -g display-time 3000
set-option -g display-panes-time 3000

# Activity Monitoring
setw -g monitor-activity on
set -g visual-activity off
set -g bell-action none
setw -g monitor-bell off
set -g activity-action none

# --- Catppuccin Mocha Theme ---
# Install: git clone https://github.com/catppuccin/tmux ~/.config/tmux/plugins/catppuccin/tmux
#          or via TPM: set -g @plugin 'catppuccin/tmux'
set -g @catppuccin_flavor 'mocha'

# Window look & feel
set -g @catppuccin_window_status_style "rounded"
set -g @catppuccin_window_text " #W"

# Active window highlighting: keep Catppuccin's DEFAULT colors (mauve number
# block, light name on the gray pill) and only add bold weight to the number
# and name so the focused window stands out without any recoloring.
set -g @catppuccin_window_current_number "#[bold]#I"
set -g @catppuccin_window_current_text "#[bold] #W"
set -g @catppuccin_window_default_fill "none"
set -g @catppuccin_window_current_fill "all"


# Status bar layout (catppuccin v2: load plugin directly, then set status-left/right)
set -g @catppuccin_date_time_text "%H:%M"
set -g status-justify absolute-centre
set -g status-left-length 100
set -g status-right-length 100

# Load catppuccin directly so module variables are initialized before status-right is set
run '~/.config/tmux/plugins/catppuccin/tmux/catppuccin.tmux'

set -g status-left ""
set -ag  status-left  "#{E:@catppuccin_status_host}"
set -ag  status-left  "#{E:@catppuccin_status_session}"
set -g status-right ""
set -agF status-right "#{E:@catppuccin_status_cpu}"
set -agF status-right "#{E:@catppuccin_status_ram}"
set -ag  status-right "#{E:@catppuccin_status_date_time}"

# tmux-cpu must run AFTER status-right is composed so it can rewrite #{cpu_percentage}
# and #{ram_percentage} literals into their shell-command equivalents.
run '~/.config/tmux/plugins/tmux-cpu/cpu.tmux'

# --- TPM Plugin Manager ---
# Install TPM: git clone https://github.com/tmux-plugins/tpm ~/.tmux/plugins/tpm
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'tmux-plugins/tmux-cpu'
set -g @plugin 'catppuccin/tmux'
set -g @plugin 'christoomey/vim-tmux-navigator'

run '~/.tmux/plugins/tpm/tpm'