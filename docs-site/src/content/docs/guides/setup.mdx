---
title: Setup
description: Getting started with dotfiles.
---

This is a starter page authored directly in the site (`source: "native"`).

Add more pages by editing `docs.manifest.json` in the docs package directory, then
re-running the generator (or `setup-docs.sh` in symlink mode).
