name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint-shell:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install shellcheck
        run: sudo apt-get update && sudo apt-get install -y shellcheck

      - name: Lint shell scripts
        run: |
          # Lint all shell scripts in modules and lib directories
          find modules lib -name "*.sh" -type f -print0 | xargs -0 shellcheck --severity=warning || true
          # Exit with error if any errors found (not warnings)
          find modules lib -name "*.sh" -type f -print0 | xargs -0 shellcheck --severity=error

  unit-tests:
    needs: lint-shell
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - name: Run unit tests
        run: go test -v -race ./...

      - name: Vet
        run: go vet ./...

  integration-tests:
    needs: unit-tests
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu, arch]
    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build test image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: test/integration/Dockerfile.${{ matrix.os }}
          tags: dotfiles-test-${{ matrix.os }}
          load: true
          cache-from: type=gha,scope=${{ matrix.os }}
          cache-to: type=gha,mode=max,scope=${{ matrix.os }}

      - name: Run integration tests
        run: docker run --rm dotfiles-test-${{ matrix.os }}
